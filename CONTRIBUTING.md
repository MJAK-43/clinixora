# Contribution — Clinixora

## Branches

- `main` : stable.
- `feature/phase-NN-description` : une phase du document `docs/phases/`.

## Avant une fusion

1. `php artisan test`
2. Revue humaine selon checklist de la phase concernée
3. Pas de secret dans les fichiers commités

## Cursor / IA

Suivre `AGENTS.md` ; chaque incrément doit rester aligné avec `docs/phases/`.
